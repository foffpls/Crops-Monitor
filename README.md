# 🌾 Graintrade Monitor Bot

Telegram бот для моніторингу та аналізу ринку зернових культур на біржі Graintrade.com.ua.

## 📋 Опис

Бот надає детальну аналітику ринку зернових культур, включаючи:
- Статистику цін (середня, мінімальна, максимальна)
- Динаміку цін за періоди
- Порівняльний аналіз між покупцями та продавцями
- Фільтрацію даних за роком
- Запити на додавання нових категорій культур

## 🚀 Швидкий старт

### Вимоги

- Python 3.10+
- Telegram Bot Token
- Доступ до інтернету

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd Graintrade_Monitor
```

2. Створіть віртуальне середовище:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# або
venv\Scripts\activate  # Windows
```

3. Встановіть залежності:
```bash
pip install -r requirements.txt
```

4. Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

5. Заповніть `.env` файл:
```env
BOT_TOKEN=your_telegram_bot_token
USD_RATE=42
ADMIN_USER_ID=your_telegram_user_id
MAX_PAGES=10
```

6. Запустіть бота:
```bash
python -m app.main
```

## 📁 Структура проекту

```
Graintrade_Monitor/
├── app/
│   ├── bot/
│   │   ├── analytics.py       # Аналіз даних та статистика
│   │   ├── crops_list.py      # Список доступних культур
│   │   ├── handlers.py        # Обробники команд та callback
│   │   ├── keyboards.py       # Клавіатури для бота
│   │   └── parser.py          # Парсинг даних з сайту
│   ├── utils/
│   │   ├── __init__.py     
│   │   └── formatters.py      # Функції форматування тексту
│   ├── config_loader.py       # Завантаження конфігурації
│   └── main.py                # Точка входу
├── data/                      # Дані (якщо потрібно)
├── .env                       # Змінні оточення (не комітиться)
├── .gitignore
├── requirements.txt           # Залежності Python
└── README.md                  # Документація
```

## 🎯 Команди бота

- `/start` - Початок роботи з ботом
- `/monitor` - Аналітика культур за весь доступний період
- `/monitor_2025` - Аналітика культур за 2025 рік
- `/add_category` - Запропонувати нову категорію культур

## ⚙️ Конфігурація

### Змінні оточення (.env)

| Змінна | Опис | Обов'язкова |
|--------|------|-------------|
| `BOT_TOKEN` | Telegram Bot Token | ✅ |
| `USD_RATE` | Курс долара для конвертації | ✅ |
| `ADMIN_USER_ID` | ID адміністратора для отримання запитів | ✅ |
| `MAX_PAGES` | Максимальна кількість сторінок для парсингу | ❌ (за замовчуванням: 10) |

## 📊 Функціонал

### Аналітика

Бот надає детальну аналітику для кожної культури:

- **Статистика цін**: середня, медіана, мінімальна, максимальна
- **Динаміка**: зміна цін за періоди (сьогодні, 3 дні, 7 днів)
- **Тренди**: напрямок змін цін
- **Порівняння**: аналіз між покупцями та продавцями

### Кешування

Дані кешуються для швидшого доступу та зменшення навантаження на сайт.

## 🛠 Технології

- **aiogram 3.23.0** - Асинхронний фреймворк для Telegram ботів
- **aiohttp 3.9.4** - Асинхронний HTTP клієнт
- **lxml** - Парсинг HTML
- **python-dotenv** - Робота з змінними оточення

## 🔄 Оновлення

Для отримання останніх оновлень:
```bash
git pull origin main
pip install -r requirements.txt --upgrade
```

## ⚠️ Примітки

- Бот парсить публічні дані з сайту Graintrade.com.ua
- Рекомендується не зловживати запитами до сайту
- Дані оновлюються при кожному запиті аналітики
